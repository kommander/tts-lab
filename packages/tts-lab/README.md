# TTS Lab App

Private OpenTUI application workspace. Run it from the repository root with `bun start`; persistent data remains in the root `.tts-lab` directory unless `TTS_LAB_HOME` is set. Generic runtime and FluidAudio facilities come from `kokoro-local-runtime/core` and `kokoro-local-runtime/fluidaudio`; no separate workspace support packages are required.
